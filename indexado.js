"use strict";
// functions
function add(a, b) {
    return a + b;
}
let numbersAdded = (a, b) => {
    return a + b;
};
let hero;
function getHero() {
    return "thor";
}
hero = getHero();
let heros = ["spiderman", "volwerine", "superman"];
heros.map((hero) => {
    return `The hero is ${hero}`;
});
function consoleError(erromsg) {
    heros.map((hero) => {
        return `The hero is ${hero}`;
    });
}
// objects
function createCourse() {
    return { name: "react", price: 70 };
}
function createUser(user) { }
const superHeros = [];
superHeros.push("superman");
const MLModels = [
    [255, 2],
    [5, 9, 0]
];
let maria = {
    username: "maria",
    id: 123
};
const user2 = ["1", 1, "ola"];
const dani = {
    dbId: 20,
    email: "dani@gmail.com",
    userId: 123,
    startTrail: () => {
        return "trail started";
    }
};
